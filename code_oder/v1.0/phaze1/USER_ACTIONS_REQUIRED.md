# Phase 1 — User Actions Required

No user action required to accept the Phase 1 baseline itself.

Optional follow-ups (not blockers for Phase 1 completion):

1. Confirm the deployed/dev backend the frontend will call next already exposes the session+CSRF auth contract before approving Phase 2.
2. Confirm whether Excel import (`excel-plan` / `lines/bulk`) should be restored in a future backend contract or removed in Phase 8 cleanup — those paths are absent from the current OpenAPI.
3. Review `code_oder/v1.0/phaze1/REPORT.md` obsolete-assumption list before starting Phase 2.
