Execute Ratab Frontend v1.0 Phase 12 from the current repository state only after Backend Phase 12 contracts are final and synced.

Primary instruction:

`code_oder/v1.0/phaze12/FRONTEND_PHASE_12_INSTRUCTIONS.md`

First read:

`code_oder/v1.0/phaze12/AGENT_READ_ORDER.md`

Critical corrected product rules:

- one root Superuser;
- only Superuser sees and uses Admin-management UI;
- promotion is by exact phone lookup;
- Superuser assigns granular optional capabilities;
- ticket view/reply is mandatory for every active Admin and shown as non-removable;
- all Admins see Support and can reply;
- advanced ticket controls and every other section are capability-driven;
- never infer platform access from company roles;
- regenerate API types, do not hand-edit schema;
- do not modify backend/commit/push.

Implement focused frontend, run focused tests, typecheck, lint and build. Stop after Frontend Phase 12.
